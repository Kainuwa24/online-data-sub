<?xml version="1.0" encoding="utf-8"?>
<!-- Production shell only loads HTTPS (Railway). Block cleartext by default. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
